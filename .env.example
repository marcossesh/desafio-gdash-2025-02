# .env.example - Copie para .env e preencha com seus dados
# IMPORTANTE: NUNCA fazer commit do arquivo .env com secrets!

# BACKEND CONFIGURATION

# Node environment (production/development)
NODE_ENV=development

# MongoDB Connection
# Formato: mongodb://username:password@host:port/database?authSource=admin
MONGODB_URI=mongodb://gdash_user:gdash_password_secure@localhost:27017/gdash_weather?authSource=admin

# JWT Secret (mínimo 32 caracteres - SUPER IMPORTANTE!)
# Gere com: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-minimum-32-characters-required

# Gemini API Key (para IA - Open)
# Obtenha em: https://ai.google.dev/
GEMINI_API_KEY=your-gemini-api-key-here

# Admin padrão (criado automaticamente no bootstrap)
DEFAULT_ADMIN_EMAIL=admin@gdash.com
DEFAULT_ADMIN_PASSWORD=admin123456

# FRONTEND CONFIGURATION

# Backend API URL (usado pelo frontend)
VITE_API_URL=http://localhost:3001

# DATABASE (Seed)

# Coordenadas para weather (Brasília, DF)
WEATHER_LATITUDE=-15.79
WEATHER_LONGITUDE=-47.88

# Intervalo de coleta de dados (segundos)
WEATHER_INTERVAL=60

# RABBITMQ

RABBITMQ_HOST=localhost
RABBITMQ_USER=guest
RABBITMQ_PASSWORD=guest

# NOTAS DE SEGURANÇA:
# 1. JWT_SECRET deve ter MÍNIMO 32 caracteres
# 2. Altere DEFAULT_ADMIN_PASSWORD em produção
# 3. Use variáveis de ambiente em vez de hardcoding
# 4. Não commite este arquivo se tiver secrets reais
# 5. Em produção, use gerenciador de secrets (AWS Secrets Manager, etc)
